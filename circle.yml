machine:
  node:
    version: 0.12.2

  pre:
    - git config --global user.name "Test"
    - git config --global user.email "test@example.com"


dependencies:
  override:
    - cd test && npm install


test:
  override:
    - cd test && npm test
